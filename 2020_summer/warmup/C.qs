namespace Solution {
    open Microsoft.Quantum.Intrinsic;

    // prepare the state |01> + |10> + |11>
    // this is an example of post-selection
    // we first prepare the equal superposition
    // and then select the states that we need

    operation post_select (qs : Qubit[]) : Unit {

    }
}